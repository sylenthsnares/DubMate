# DubMate Pack Builder — AI Processing Pipeline (Optional)
# Install with: pip install -r requirements_builder.txt
# Requires ~2 GB download (PyTorch with CUDA GPU acceleration + Demucs + Whisper)

torch>=2.0.0
torchaudio>=2.0.0
demucs>=4.0.0
openai-whisper>=20231117
pykakasi>=2.3.0
# Released wheel, not the master sdist: the embedded Python cannot run PEP 517
# build backends, and a source install of yt-dlp fails with BackendUnavailable.
yt-dlp>=2025.1.1
